spring:
  config:
    activate:
      on-profile: integration-test
  docker:
    compose:
      enabled: false
  datasource:
    hikari:
      connection-timeout: 1000

mykytadu:
  identity:
    email:
      delivery-mode: accept
